name: Generate Wiki Documentation

on:
  push:
    branches: [main, master]
    paths: ['README.md', 'docs/**', 'src/**', '*.md']
  workflow_dispatch:

permissions:
  contents: write       # Need write to push changes
  pages: write
  id-token: write

jobs:
  generate-wiki:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - run: pip install git-wiki-builder

      # Clone the wiki repo using PAT
      - name: Clone wiki repo
        run: |
          git clone https://${{ secrets.WIKI_ACTION_TOKEN }}@github.com/${{ github.repository }}.wiki.git wiki
        env:
          WIKI_ACTION_TOKEN: ${{ secrets.WIKI_ACTION_TOKEN }}

      # Run git-wiki-builder with env telling it the wiki dir
      - run: git-wiki-builder wiki
        env:
          GITHUB_TOKEN: ${{ secrets.WIKI_ACTION_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      # Push changes to the wiki repo
      - name: Push changes to wiki
        run: |
          cd wiki
          git add .
          git commit -m "Update wiki via GitHub Action" || echo "No changes to commit"
          git push
        env:
          WIKI_ACTION_TOKEN: ${{ secrets.WIKI_ACTION_TOKEN }}


# name: Generate Wiki Documentation

# on:
#   push:
#     branches: [main, master]
#     paths: ['README.md', 'docs/**', 'src/**', '*.md']
#   workflow_dispatch:

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# jobs:
#   generate-wiki:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#       - uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
#       - run: pip install git-wiki-builder
#       - run: git-wiki-builder
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           GITHUB_REPOSITORY: ${{ github.repository }}